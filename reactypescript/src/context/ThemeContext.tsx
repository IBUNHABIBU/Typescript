import { actionType, StateType } from "../types/types";

const INITIAL_STATE = {
    theme: "dark",
    fontSize: 16,
}

const reducer = (state: StateType, action: actionType) => {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}