const INITIAL_STATE = {
    theme: "dark",
    fontSize: 16,
}

const reducer = (state, action) => {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}